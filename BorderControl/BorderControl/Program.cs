using System;
using System.Collections.Generic;
using System.Linq;
namespace BorderControl {
    class Program {
        static void Main(string[] args)
        {
            Console.WriteLine("Enter your {ID}, {Age},  or {Model}, {ID} here: ");
            List<Identifiable> check = new List<Identifiable>();
            string Command = Console.ReadLine();
            while(Command != "End")
            {
                string[] line = Command.Split; 
            }
        }
    }
}
